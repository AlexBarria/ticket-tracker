# ==============================================
# Ticket Tracker - Environment Variables
# ==============================================
# Copy this file to .env and fill in your actual values

# ----------------------------------------------
# PostgreSQL Database Configuration
# ----------------------------------------------
POSTGRES_USER=your_db_username
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=tickets_db

# ----------------------------------------------
# MinIO (S3-compatible storage) Configuration
# ----------------------------------------------
MINIO_ROOT_USER=your-minio-user
MINIO_ROOT_PASSWORD=your_minio_password
MINIO_BUCKET=ticket-tracker

# ----------------------------------------------
# MLflow Configuration
# ----------------------------------------------
# MLflow will use the Postgres and MinIO credentials above
# The database will be created on the fly if it doesn't exist
MLFLOW_DB=mlflow_db 

# ----------------------------------------------
# OpenAI API Configuration
# ----------------------------------------------
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# ----------------------------------------------
# Tavily API (Web Search Tool)
# ----------------------------------------------
TAVILY_API_KEY=your_tavily_api_key_here

# ----------------------------------------------
# Guardrails Configuration
# ----------------------------------------------
GUARDRAILS_ENABLED=true
GUARDRAILS_TOKEN=your_guardrails_token_here

# ----------------------------------------------
# Auth0 Configuration
# ----------------------------------------------
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your_auth0_client_id
AUTH0_CLIENT_SECRET=your_auth0_client_secret

AUTH0_USER_MAIL=user@email.com
AUTH0_USER_PASSWORD=your_user_password

AUTH0_ADMIN_MAIL=admin@email.com
AUTH0_ADMIN_PASSWORD=your_admin_password

# ----------------------------------------------
# Evaluation Service Configuration
# ----------------------------------------------
EVAL_SCHEDULE_CRON=0 2 * * *
EVAL_BATCH_SIZE=5

# ----------------------------------------------
# Notes:
# ----------------------------------------------
# 1. Get your OpenAI API key from: https://platform.openai.com/api-keys
# 2. Get your Auth0 credentials from: https://manage.auth0.com/
# 3. Get your Tavily API key from: https://tavily.com/
# 4. PostgreSQL credentials can be customized (change in both POSTGRES_* and DATABASE_URL)
# 5. MinIO credentials can be customized for production (default: minioadmin/minioadmin)
# 6. Guardrails token can be generated from: https://guardrails.ai/
# 7. Ensure to keep this file secure and do not expose sensitive information
# 8. After editing, save this file as `.env` in the root directory of the project
# ==============================================